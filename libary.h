#pragma once
#include <iostream>      // cout, cin, endl
#include <string>        // std::string
#include <vector>        // std::vector
#include <list>          // std::list
#include <map>           // std::map
#include <set>           // std::set
#include <unordered_map> // std::unordered_map
#include <unordered_set> // std::unordered_set
#include <algorithm>     // sort, find, transform
#include <fstream>       // file I/O: ifstream, ofstream
#include <sstream>       // stringstream for parsing strings
#include <iomanip>       // formatting: setw, setprecision
#include <ctime>         // for time_t, time(), localtime
#include <chrono>        // precise timing with std::chrono
#include <cmath>         // math functions like pow, sqrt
#include <cstdlib>       // general utilities, rand, system
#include <random>        // better random generation
#include <stdexcept>     // runtime_error, invalid_argument
#include <cctype>        // char checks: isdigit, isalpha
#include <functional>    // std::function, lambdas
#include <utility>       // std::pair, std::make_pair
#include <exception>     // base exception class
using namespace std;
class library
{
};

